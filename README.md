# 企业自动化演示站

这是一个适合影刀 RPA 捕获与流程演示的纯静态电商后台，可直接部署到 Netlify。

## 本地运行

双击 `start_server.bat`，浏览器会打开 `http://127.0.0.1:8899/`。

## Netlify 部署

- Build command：留空
- Publish directory：`.`
- 项目已包含 `netlify.toml`

## 常用演示流程

1. 选择“天猫旗舰店”和“衣物清洁”。
2. 点击“查询数据”。
3. 读取结果数量和核心指标。
4. 点击“导出 CSV”，验证下载文件。

筛选框和按钮均保留稳定的 HTML `id`，方便影刀重复捕获。后续为其他企业改版时，优先替换品牌文案、颜色和示例数据，不修改已有控件 `id`。
